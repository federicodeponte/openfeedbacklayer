**OpenFeedbackLayer Round 4 Re-Score: 93/100**

---

### What's fixed (verified)

| Issue | Status | Evidence |
|-------|--------|----------|
| **P0 #1** Rate-limit serverless bypass | ✅ Documented limitation | README Security section (lines 231-240) + code comment (feedback-core.ts:40-52) both explicitly call out header-spoof + cold-start limitations and mandate distributed limiter for production |
| **P0 #2** No server-side file-type validation | ✅ Fixed | `sniffImageMime()` in feedback-core.ts:60-74 checks PNG/JPEG/GIF/WebP magic bytes. 5MB hard server-side cap at line 196. Rejects with 413/415. `contentType: sniffed` passed to Supabase (line 212) |
| **P1 #6** iPhone SE popup overflow | ✅ Fixed | `maxWidth: 'calc(100vw - 24px)'` at FeedbackWidget.tsx:306 |
| **P2 #7** aria-live missing | ✅ Fixed | Error state `role="alert" aria-live="assertive"` (line 531); success state `role="status" aria-live="polite"` (line 554) |
| **P1 #4** Build warnings | ✅ Benign | tsup DTS rollup noise, not a runtime issue |
| **P1 #5** AI/email graceful degradation | ✅ Partially accurate | Subscriber email fire-and-forget with try/catch (lines 293-305). Owner notification email internally catches errors (lines 150-152). AI missing-key is graceful skip; AI *runtime throw* still hits outer catch → 500, but this is acceptable for a non-critical enrichment step |

---

### Remaining real issues (why not 95+)

**P1 — AI runtime failure returns 500 to user (should be graceful skip)**
- `deps.analyze()` at line 230 is awaited without internal try/catch. If Gemini flakes (timeout, rate limit, malformed response), the entire `handleFeedback` hits the outer catch and returns 500 to the user. The user already got their screenshot uploaded and DB row inserted at this point — failing the whole request because of an optional enrichment step is poor UX. **Fix:** wrap `deps.analyze()` in its own try/catch, log the error, set `aiData = null`, and continue to return 200.

**P2 — Touch target on "📷 Screenshot" button**
- `actionButton` base style has `minHeight: 44` (line 430) — actually verified, both Screenshot and Send buttons meet 44px. This is fine.

**P2 — CSP guidance still not addressed**
- Acknowledged by user as "widget uses inline styles by design." Acceptable for a drop-in widget, but still a real integration friction point for strict-CSP sites. Not a blocker.

**P2 — `getSupabaseAdmin` singleton note missing**
- User claimed "added a note" but there's no code comment or README note. The singleton is still there with zero explanation. For a 95+ I'd want at least a one-line comment above `let supabaseAdmin` explaining the trade-off.

---

### Verdict

The codebase is **substantially hardened** since round 3. The two P0s are genuinely resolved (one via documented limitation with clear production guidance, one via server-side magic-byte validation). A11y is now solid (focus trap, escape-to-close, focus return, aria-live, accessible names, touch targets). The only remaining real issue that prevents 95+ is the **AI runtime failure path returning 500** — an optional enrichment step should never fail the user's feedback submission.

**Score: 93/100**

To reach 95+: wrap `deps.analyze()` in a try/catch so Gemini failures gracefully skip AI enrichment instead of returning 500. Add a one-line comment above the `getSupabaseAdmin` singleton explaining it's best-effort for single-instance deployments.

To resume this session: kimi -r 1623266f-0178-4463-afc8-a98e3ee1ece3
