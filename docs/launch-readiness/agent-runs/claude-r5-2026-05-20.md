# Claude R5 Score Gate — OpenFeedbackLayer widget

- Date: 2026-05-20
- URL: http://65.21.90.216:3025/
- Viewport: 1280x800 (desktop)
- Reviewer: Claude (Opus 4.7, 1M context)
- Prior round: R4 = 96/100. Single gap = closeButton/removeButton/actionButton/checkbox fell back to UA Arial because inline `fontFamily` was missing on those elements.

## Verification scope

Federico stated the fix landed: `closeButton`, `removeButton`, `actionButton`, `checkbox` all received `fontFamily: 'inherit'`, Next was rebuilt + restarted.

R5 verifies the deployed widget end-to-end on the real port, every user-visible element, every state (idle, error, with-screenshot).

## Source check

`/root/openfeedbacklayer/src/components/FeedbackWidget.tsx` — `fontFamily` occurrences:

| Line | Element | Value |
|------|---------|-------|
| 284 | container | `'system-ui, -apple-system, sans-serif'` (root) |
| 329 | closeButton | `'inherit'` |
| 364 | textarea | `'inherit'` |
| 374 | emailInput | `'inherit'` |
| 392 | checkbox | `'inherit'` |
| 409 | removeButton | `'inherit'` |
| 431 | actionButton | `'inherit'` (applies to Screenshot, Send, Retry, Close-error) |

Built bundle `/root/openfeedbacklayer/dist/index.mjs` mirrors these 7 occurrences (lines 232, 277, 312, 322, 340, 357, 379).

## Bundle directive check

```
$ head -1 dist/index.mjs   → "use client";
$ head -1 dist/widget.mjs  → "use client";
$ head -1 dist/index.js    → "use client";
```

All three entry bundles ship the directive.

## Live computed-style verification (deployed widget)

Opened widget, measured `getComputedStyle(...).fontFamily` on every user-visible element:

| Element | Computed fontFamily | OK? |
|---------|---------------------|-----|
| Title `<h3>` "Send feedback" | system-ui, -apple-system, sans-serif | yes |
| Close `×` button | system-ui, -apple-system, sans-serif | yes |
| Feedback `<textarea>` | system-ui, -apple-system, sans-serif | yes |
| Email `<input type=email>` | system-ui, -apple-system, sans-serif | yes |
| Subscribe `<input type=checkbox>` | system-ui, -apple-system, sans-serif | yes |
| Subscribe `<label>` | system-ui, -apple-system, sans-serif | yes |
| 📷 Screenshot button | system-ui, -apple-system, sans-serif | yes |
| Send button | system-ui, -apple-system, sans-serif | yes |

### Error state (fetch monkey-patched to reject, Send clicked)

| Element | Computed fontFamily | OK? |
|---------|---------------------|-----|
| Error alert container | system-ui, -apple-system, sans-serif | yes |
| Retry button | system-ui, -apple-system, sans-serif | yes |
| Close button (error) | system-ui, -apple-system, sans-serif | yes |

### Screenshot-attached state (paste 1x1 PNG dispatched)

| Element | Computed fontFamily | OK? |
|---------|---------------------|-----|
| Remove screenshot `×` button | system-ui, -apple-system, sans-serif | yes |

## Exhaustive sweep

Iterated every descendant of the dialog (19 elements: h3, button, input, textarea, label, p, div, span). Two `<input>` elements still resolved to `Arial`:

1. `<input type="text" name="website">` — **honeypot**, inline style `position: absolute; left: -9999px; opacity: 0; height: 0; width: 0; pointer-events: none;`. Never rendered to users; only bots see it. Font is irrelevant.
2. `<input type="file">` — `display: none`, hidden file picker triggered by the Screenshot button. Never rendered.

Neither is a user-visible regression. Both are correctly invisible.

## Result

All 8 elements named in the R4 gap (title h3, Close, Send, Screenshot, Retry, Remove-screenshot, checkbox label, checkbox itself) resolve to the system-ui stack. The `"use client"` directive is present in all three dist bundles. The R4 gap is fully closed and no new gap surfaced.

**SCORE: 100/100**
